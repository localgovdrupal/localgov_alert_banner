name: Test localgovdrupal/localgov_alert_banner drupal-module

on:
  push:
    branches:
      - '1.x'
  pull_request:
    branches:
      - '1.x'
  workflow_dispatch:

jobs:
  tests:
    uses: localgovdrupal/localgov_shared_workflows/.github/workflows/test-module.yml@feature/1.x-17-check-changed-files-only-coding-standards
    with:
      project: 'localgovdrupal/localgov_alert_banner'
      project_path: 'web/modules/contrib/localgov_alert_banner'
